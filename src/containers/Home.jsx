import React from 'react';

import { PostList } from '../components';

import { Container } from '../shared';

const Home = () => (
  <main>
    <Container>
      <PostList />
    </Container>
  </main>
);

export default Home;
